[project]
name = "openinterview-schemas"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pydantic[email]>=2.6"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
